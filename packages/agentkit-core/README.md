# agentkit-core

The protocol layer + default Runtime for [AgentKit](https://github.com/agentkit-dev/agentkit).

This package alone is enough to **build** an Agent in Python; install built-in Techniques separately
(e.g. `pip install agentkit-builtin-planner-react`) to actually **run** something useful.
